apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: boutique-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: prod-boutique.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80